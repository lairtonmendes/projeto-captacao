<%= form_for(@coordenada) do |f| %>
  <% if @coordenada.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coordenada.errors.count, "error") %> prohibited this coordenada from being saved:</h2>

      <ul>
      <% @coordenada.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ot %><br>
    <%= f.number_field :ot %>
  </div>
  <div class="field">
    <%= f.label :lat %><br>
    <%= f.text_field :lat %>
  </div>
  <div class="field">
    <%= f.label :lon %><br>
    <%= f.text_field :lon %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
